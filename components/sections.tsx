import Link from 'next/link';import Image from 'next/image';import {ArrowRight,Check,ShieldCheck,Users,MapPin,Phone} from 'lucide-react';import {Button} from './ui/button';import {business,serviceImages} from '@/lib/data';
export function Eyebrow({children}:{children:React.ReactNode}){return <p className="mb-3 text-xs font-black uppercase tracking-[.22em] text-oxide">{children}</p>}
const heroImageFor=(title:string)=>{const t=title.toLowerCase();if(t.includes('gutter installation')||t.includes('seamless gutters'))return{src:'/service-images/gutter-installation.webp',alt:'Professional installing seamless gutters on a North Texas home'};if(t.includes('leaks')||t.includes('sagging'))return{src:'/service-images/gutter-repair.webp',alt:'Technician repairing a residential gutter connection'};if(t.includes('less debris'))return{src:'/service-images/gutter-guards.webp',alt:'Micro-mesh gutter guards being installed'};if(t.includes('clear the path'))return{src:'/service-images/gutter-cleaning.webp',alt:'Professional removing leaves during gutter cleaning'};if(t.includes('durable finish'))return{src:'/service-images/exterior-painting.webp',alt:'Exterior siding and trim being professionally painted'};if(t.includes('fresh color')||t.includes('cleaner process'))return{src:'/service-images/interior-painting.webp',alt:'Professional interior painting with the room carefully protected'};if(t.includes('transform cabinets'))return{src:'/service-images/cabinet-painting.webp',alt:'Cabinet doors prepared for a professional painted finish'};if(t.includes('straight answers'))return{src:'/service-images/roofing.webp',alt:'Professional inspecting an architectural shingle roof'};if(t.includes('privacy'))return{src:'/service-images/fencing.webp',alt:'Craftsman installing a cedar privacy fence and gate'};return undefined};
export function PageHero({eyebrow,title,desc,image}:{eyebrow:string,title:string,desc:string,image?:{src:string;alt:string}}){const visual=image||heroImageFor(title);return <section className="relative overflow-hidden bg-ink px-4 py-20 text-white md:py-28">{visual&&<><Image src={visual.src} alt={visual.alt} fill priority sizes="100vw" className="object-cover object-center"/><div className="absolute inset-0 bg-gradient-to-r from-ink via-ink/90 to-ink/25"/></>}<div className={`grain absolute inset-0 ${visual?'opacity-20':'opacity-100'}`}/><div className="relative mx-auto max-w-7xl"><Eyebrow>{eyebrow}</Eyebrow><h1 className="max-w-4xl font-display text-4xl uppercase leading-[.95] md:text-7xl">{title}</h1><p className="mt-6 max-w-2xl text-lg text-white/80">{desc}</p><div className="mt-8 flex flex-wrap gap-3"><Button asChild><Link href="/contact">Get a free estimate <ArrowRight size={16}/></Link></Button><Button variant="outline" asChild><a href={`tel:${business.phoneHref}`}><Phone size={16}/> Call {business.phone}</a></Button></div></div></section>}
export function TrustBar(){return <section className="bg-white px-4 py-6"><div className="mx-auto grid max-w-7xl gap-4 text-sm font-black uppercase tracking-wider sm:grid-cols-3"><div className="flex items-center gap-3"><MapPin className="text-oxide"/>Local to DFW</div><div className="flex items-center gap-3"><Users className="text-oxide"/>Family-owned</div><div className="flex items-center gap-3"><ShieldCheck className="text-oxide"/>Veteran-owned</div></div></section>}
export function ServiceCard({name,desc,href,index}:{name:string,desc:string,href:string,index?:string}){const visual=serviceImages[href.replace(/^\//,'')];return <article className="brand-shadow group overflow-hidden border border-ink/15 bg-white">{visual&&<div className="relative aspect-[16/10] overflow-hidden"><Image src={visual.src} alt={visual.alt} fill sizes="(min-width: 1024px) 25vw, (min-width: 640px) 50vw, 100vw" className="object-cover transition-transform duration-500 group-hover:scale-105"/></div>}<div className="p-6"><div className="mb-8 flex items-center justify-between"><span className="font-display text-4xl text-steel">{index||'→'}</span><ArrowRight className="text-oxide transition-transform group-hover:translate-x-1"/></div><h3 className="font-display text-2xl uppercase">{name}</h3><p className="mt-3 text-ink/70">{desc}</p><Link className="focus-ring mt-6 inline-block font-black uppercase text-oxide" href={href}>Explore service</Link></div></article>}
export function Checklist({items}:{items:string[]}){return <ul className="grid gap-3 sm:grid-cols-2">{items.map(i=><li className="flex gap-3" key={i}><Check className="mt-0.5 shrink-0 text-oxide" size={20}/><span>{i}</span></li>)}</ul>}
export function CTA({title='Protect your home before the next Texas storm.',label='Get your free gutter estimate',eyebrow='We Gutter Done Right.'}:{title?:string;label?:string;eyebrow?:string}){return <section className="bg-oxide px-4 py-16 text-ink"><div className="mx-auto flex max-w-7xl flex-col items-start justify-between gap-8 md:flex-row md:items-center"><div><p className="text-xs font-black uppercase tracking-[.2em] text-ink/70">{eyebrow}</p><h2 className="mt-2 max-w-3xl font-display text-3xl uppercase md:text-5xl">{title}</h2></div><Button variant="light" asChild><Link href="/contact">{label}</Link></Button></div></section>}
