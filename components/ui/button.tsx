import {Slot} from '@radix-ui/react-slot';import {cva,type VariantProps} from 'class-variance-authority';import {cn} from '@/lib/utils';import * as React from 'react';
const variants=cva('focus-ring inline-flex min-h-12 items-center justify-center gap-2 px-5 py-3 text-sm font-black uppercase tracking-wider transition-colors disabled:cursor-not-allowed disabled:opacity-60',{variants:{variant:{default:'bg-oxide text-ink hover:bg-oxide-dark',outline:'border-2 border-current hover:bg-white hover:text-ink',light:'bg-white text-ink hover:bg-steel'}},defaultVariants:{variant:'default'}});
export function Button({className,variant,asChild=false,...p}:React.ButtonHTMLAttributes<HTMLButtonElement>&VariantProps<typeof variants>&{asChild?:boolean}){const C=asChild?Slot:'button';return <C className={cn(variants({variant}),className)} {...p}/>}
